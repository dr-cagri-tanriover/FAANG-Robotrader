[project]
name = "faang-robotrader"
version = "1.0.0"
description = "Automated AI-driven virtual trading simulator benchmarking the FAANG Pulse AI model across four investor personas"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "gradio>=6.9.0",
    "gradio_client>=2.3.0"
]
